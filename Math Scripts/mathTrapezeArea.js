let trapezeShortWidth = 24;
let trapezeLongWidth = 72;
let trapezeheight = 40;

const trapezeArea = ((trapezeShortWidth + trapezeLongWidth) / 2) * trapezeheight;
console.log(`The area of the trapezoid is: ${trapezeArea}m2`);
